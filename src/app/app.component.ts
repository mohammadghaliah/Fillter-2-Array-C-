import { Component, VERSION } from '@angular/core';

@Component({
  selector: 'my-app',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'],
})
export class AppComponent {
  // var newIdentityProvider = identityProvider.Where(att => externalProviderUsers.Select(at => at.Provider).Contains(att.TypeCode));
}
